# Remove duplicate and unnecessary packages, and ensure compatibility with your app and Python version
# Recommended requirements.txt for your Gradio Azure OpenAI app

gradio
openai>=1.0.0
python-dotenv
azure-identity
azure-ai-inference
Pillow
requests
pandas
numpy
opencv-contrib-python
scikit-image
torch
torchvision
transformers
ftfy
regex
tqdm

# For CLIP-based metrics
git+https://github.com/openai/CLIP.git

# Image Quality Assessment (IQA) libraries
piq                    # BRISQUE, NIQE, and other IQA metrics
torchmetrics[multimodal]  # CLIPScore from torchmetrics

# Note: pyiqa removed due to dependency conflicts with tokenizers/transformers
# The code has fallbacks for CLIP-IQA when pyiqa is not available